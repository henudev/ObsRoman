# 复制为 .env 或导出环境变量后覆盖；以下默认值仅用于本地开发联调
# OpenObserve 根用户（Dev 默认，生产必须修改）
OO_ROOT_USER_EMAIL=root@example.com
OO_ROOT_USER_PASSWORD=Complexpass#123

# Trace Log Service 的 API Key（Dev 默认，生产必须修改）
TRACE_LOG_ADMIN_KEY=dev-admin-key
TRACE_LOG_WRITER_KEY=dev-writer-key
TRACE_LOG_READER_KEY=dev-reader-key
