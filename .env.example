# 复制为 .env 或导出环境变量后覆盖；以下默认值仅用于本地开发联调
# OpenObserve 根用户（Dev 默认，生产必须修改）
OO_ROOT_USER_EMAIL=root@example.com
OO_ROOT_USER_PASSWORD=Complexpass#123

# Trace Log Service 内置管理员 Key（统一管理员权限，生产必须修改默认 Secret）
TRACE_LOG_ADMIN_KEY=admin
TRACE_LOG_ADMIN_SECRET=admin
